# 需求智能体方案实现状态对照

对照《最终需求智能体完整方案.md》V4.0，按**实施路线图**与**能力矩阵**逐项标注当前实现状态。  
**约定**：✅ = 已实现，⚠️ = 部分实现/与方案有差异，❌ = 未实现。

---

## 一、Phase 0（Week 1-2）

| 方案条目 | 状态 | 说明 |
|----------|------|------|
| 确定模型供应商（Kimi/DeepSeek 密钥） | ✅ | 使用 `KIMI_API_KEY`、DeepSeek 备用，见 `agent_router` |
| `AiRequirementTask` + `PromptVersion` + `AiRequirementFeedback` + `AiAuditLog` 数据模型 Migration | ✅ | `apps/ai_requirement/models.py` 及 migrations 已存在 |
| 7 个任务的 **Pydantic Schema** 输出规范 | ✅ | `services/schemas.py` 含全部 7 类 Schema（含 AgenticPRDSchema、FeatureBreakdownSchema 等） |
| 与用例智能体对齐 `feature_breakdown` 输出格式（方式 A/B） | ⚠️ | `feature_breakdown` 任务类型已移除；桥接仅返回 `requirement_text` + `source_task_id`，用例侧走 `generate-stream` |
| 编写评估数据集初稿（prd_draft + feature_breakdown 各 10 条） | ⚠️ | `EvalDataset` 模型与接口已有；是否已录入各 10 条 Ground Truth 需看数据 |
| SecurityMiddleware 基础版（Prompt 注入检测 + PII 脱敏） | ✅ | `services/security.py`，run_stream 前 `security.pre_process` |

---

## 二、Phase 1 核心（Week 3-6）

| 方案条目 | 状态 | 说明 |
|----------|------|------|
| 引入 instructor，实现 `run_stream`（流式）+ `run_validated`（强校验）双接口 | ✅ | `agent_router.run_stream` / `run_validated`，instructor 用于 `run_validated` |
| 实现 `prd_draft`、`feature_breakdown` 两个高优任务（Schema + Prompt v1.0.0） | ✅ | 7 个任务均有对应 Prompt 与 Schema（`prompts/` + `schemas.py`） |
| FileProcessor 多模态支持（文本+图片+文档） | ✅ | run_stream 中复用 ai_testcase 的 FileProcessor，支持 PDF/Word/图片等 |
| Web 工作台前端（角色选择、任务类型、文件上传、流式展示、👍👎 反馈按钮） | ✅ | `AiRequirementAgent.vue`：角色/任务、上传、流式展示；反馈接口与前端是否暴露 👍👎 需在页面上确认 |
| 与用例智能体跳转集成（桥接 → 用例页） | ✅ | `bridge-to-testcase` + sessionStorage 预填；用户在用例页调用 `generate-stream`（已移除 `generate-from-structure`） |
| 用例智能体完成后回写 `downstream_testcases` 到 `AiRequirementTask` | ❌ | 字段仍存在；原仅在已删除的 `generate-from-structure` 路径回写，若需血缘需在 `generate-stream` 成功路径等另行实现 |
| 实现多轮对话接口 `/chat`（置信度<0.7 时主动澄清） | ✅ | `POST /api/ai_requirement/chat/`，`chat_stream_view` 已实现 |
| 需澄清-0.7 自动处置（澄清并继续） | ✅ | `POST /api/ai_requirement/clarify-and-continue/` + 前端澄清表单，提交后流式重新生成 |

---

## 三、Phase 1 优化（Week 7-8）

| 方案条目 | 状态 | 说明 |
|----------|------|------|
| 实现剩余 5 个任务（含各自 Pydantic Schema） | ✅ | 7 个任务均有 Prompt + Schema（competitive_analysis, prd_refine, requirement_analysis, tech_design, test_requirement_analysis, prd_draft, feature_breakdown） |
| 在评估数据集上跑 Prompt v1.0.0 基线，记录 CLASSic 指标 | ✅ | `EvalRun` + `eval_runner`：遍历数据集调用 `run_validated`，写回覆盖率/幻觉率/Schema 合规率/稳定性；评估基线+回归（set_baseline、regression_detected）已实现 |
| Prompt 优化迭代（基于用户反馈 + 评估指标，发布 v1.1.0） | ⚠️ | 反馈与评估数据已有；“发布 v1.1.0” 为流程/运营，非单点功能 |
| 成本监控 Dashboard（Token 消耗统计、成本告警、用户配额） | ✅ | `GET /api/ai_requirement/stats/overview/` 含今日成本、`cost_alert`/`cost_alert_threshold_usd`；`cost_quota` 模块：超阈值写日志+可选 Webhook；用户日成本/请求上限校验，run-stream/chat 前置校验，超限 SSE `quota_exceeded`；前端看板告警条、流式接口处理配额错误 |

---

## 四、Phase 2（Month 3-4）

| 方案条目 | 状态 | 说明 |
|----------|------|------|
| FFmpeg 视频关键帧抽取服务 | ✅ | `VideoProcessor`（FFmpeg + OpenCV 抽帧），run_stream 中支持视频附件（竞品分析等） |
| LangGraph 工作流引擎集成（含 **Postgres** Checkpointer + 迭代上限） | ⚠️ | 已有 LangGraph：`prd_workflow`、`multi_agent`、executor、checkpoint；**Checkpointer 为 SQLite**（`checkpoint.py`），非方案中的 Postgres |
| Jira/Confluence 连接器（HTTP 接口） | ✅ | `sync-to-jira/`、`sync-to-confluence/` + 前端按钮；完整 MCP Server 不做 |
| RAG/长期记忆（历史 PRD、术语表） | ✅ | `RequirementChunk`、rag 检索注入、成功任务自动索引、`rebuild_rag_chunks` 管理命令 |
| 人工审批节点前端开发 + 步骤可视化 | ✅ | 深度模式/多智能体开启即显示步骤预览；el-steps 步骤条、审批面板（通过/驳回+意见）、approve 接口已打通 |
| DeepSeek-R1 双模型验证（tech_design 任务） | ✅ | `agent_router` 中 tech_design + use_thinking 可走 DeepSeek |
| LLM 调用链追踪 | ✅ / ⚠️ | 自建 `LlmCallLog` + span + stats/overview；OpenTelemetry/LangSmith 未接 |

---

## 五、方案中其他核心能力

| 能力/接口 | 状态 | 说明 |
|-----------|------|------|
| 统一入口 `POST /api/ai_requirement/run-stream` | ✅ | 支持 JSON/FormData、多模态、output_format |
| 非流式 `POST /api/ai_requirement/run`（下游消费 + Schema 校验） | ✅ | `run_sync_view` 调用 run_validated 逻辑 |
| 双形态输出（result_json + result_md，output_format） | ✅ | `_extract_result_md`，done 事件与同步接口带 `result_md` |
| 工作流持久化（Checkpointer） | ✅ | SQLite 持久化，支持恢复与 resume（与方案“Postgres”不一致见上） |
| PRD 工作流 / 多智能体工作流（start + approve） | ✅ | `workflow/start`、`workflow/<id>/approve`、`multi-agent/start`、`multi-agent/<id>/approve` |
| 用户反馈 `POST /api/ai_requirement/feedback` | ✅ | `feedback_view`，含 rating、issue_type、comment |
| Prompt 版本管理（PromptVersion + is_active） | ✅ | 模型与加载逻辑（get_prompt 按 task_type 取激活版本） |
| 评估流水线（EvalDataset、EvalRun、后台跑评估） | ✅ | EvalRunViewSet.start + eval_runner 后台执行，结果写回；含 CLASSIC 稳定性、基线+回归（set_baseline） |
| 澄清并继续（需澄清时结构化重跑） | ✅ | `POST /api/ai_requirement/clarify-and-continue/`，前端澄清表单提交后流式重新生成 |
| 安全：Prompt 注入、PII 脱敏、审计日志（AiAuditLog） | ✅ | SecurityMiddleware + AiAuditLog 写入 |

---

## 六、未实现或待补项汇总

- **方案明确未实现**
  - 完整 MCP Server（IDE/CI 协议）；Jira/Confluence 已通过 HTTP 连接器实现
  - LLM 调用链接入 OpenTelemetry / LangSmith（自建 LlmCallLog 已实现）

- **与方案有差异**
  - Checkpointer：当前为 **SQLite**，方案为 **Postgres**（可后续迁库或文档对齐）。

- **需在业务/产品侧确认**
  - 评估数据集是否已按“prd_draft + feature_breakdown 各 10 条”录入。
  - 前端是否在所有结果页显式提供 👍👎 反馈按钮并调 feedback 接口。
  - 工作流/多智能体前端是否有完整的“步骤可视化 + 人工审批”界面。

---

## 七、小结

- **Phase 0 / Phase 1 核心 / Phase 1 优化**：绝大部分已实现（数据模型、7 任务、双接口、多模态、桥接与回写、chat、反馈、评估流水线、安全、统计、成本告警与用户配额）；仅评估数据初稿、Prompt 发布流程为 ⚠️。
- **Phase 2**：视频抽帧、LangGraph 工作流与审批接口、DeepSeek 双模型、Jira/Confluence 连接器、RAG 长期记忆、自建可观测、前端步骤可视化 已实现；Postgres Checkpointer、OpenTelemetry/LangSmith 为 ⚠️ 或未接。
- **闭环**：需求 → 桥接 → 用例页预填 → `generate-stream`；`downstream_testcases` 自动回写已随 `generate-from-structure` 移除，待产品决定是否在新路径实现。

如需，可再按“优先补哪些”拆成下一版迭代清单（例如：Postgres Checkpointer、MCP、OTel/LangSmith 其一）。
